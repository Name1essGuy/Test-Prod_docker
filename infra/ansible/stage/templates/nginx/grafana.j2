# /etc/nginx/sites-enabled/prometheus

server {
    listen 80;
    listen [::]:80;
    server_name grafana.{{ domain_name }};

    location / {
        proxy_pass           http://localhost:3000/;
    }
}